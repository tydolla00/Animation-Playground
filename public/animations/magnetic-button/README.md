Place `preview.webp` (or .png/.avif/.jpg) here for the bento tile.

The seed entry currently relies on the no-preview placeholder treatment. To
add a still: open `/embed/magnetic-button` in your browser, screenshot a moment
where the button is leaning into the cursor, and save it here as
`preview.webp` (4:3 or matching the tile's `tileSize` aspect).
